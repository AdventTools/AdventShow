name: Build and Release Electron App

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            script: build:win
            artifact_name: electron-windows
          - os: macos-latest
            script: build:mac
            artifact_name: electron-macos
          - os: ubuntu-latest
            script: build:linux
            artifact_name: electron-linux

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Rebuild native modules
        run: npm rebuild

      - name: Build app
        run: npm run ${{ matrix.script }}
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
          ELECTRON_BUILDER_PUBLISH: never

      - name: Upload raw build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: release/
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Show downloaded files
        run: find artifacts -type f | sort

      - name: Rename assets to include tag version
        shell: bash
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          shopt -s globstar nullglob

          for file in artifacts/**/*.exe; do
            dir="$(dirname "$file")"
            base="$(basename "$file" .exe)"
            mv "$file" "$dir/${base}-${VERSION}.exe"
          done

          for file in artifacts/**/*.dmg; do
            dir="$(dirname "$file")"
            base="$(basename "$file" .dmg)"
            mv "$file" "$dir/${base}-${VERSION}.dmg"
          done

          for file in artifacts/**/*.AppImage; do
            dir="$(dirname "$file")"
            base="$(basename "$file" .AppImage)"
            mv "$file" "$dir/${base}-${VERSION}.AppImage"
          done

          for file in artifacts/**/*.deb; do
            dir="$(dirname "$file")"
            base="$(basename "$file" .deb)"
            mv "$file" "$dir/${base}-${VERSION}.deb"
          done

          for file in artifacts/**/*.rpm; do
            dir="$(dirname "$file")"
            base="$(basename "$file" .rpm)"
            mv "$file" "$dir/${base}-${VERSION}.rpm"
          done

      - name: Keep only release files
        run: |
          find artifacts -type f \
            ! -name "*.exe" \
            ! -name "*.dmg" \
            ! -name "*.AppImage" \
            ! -name "*.deb" \
            ! -name "*.rpm" \
            -delete

      - name: Show final release files
        run: find artifacts -type f | sort

      - name: Create GitHub Release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/**/*.exe
            artifacts/**/*.dmg
            artifacts/**/*.AppImage
            artifacts/**/*.deb
            artifacts/**/*.rpm
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
